#include <stdio.h>
#include <ctype.h>
/**
 * _islower- function taht checks for lowercase character
 * Return: 1 if character is lowercase and 0 otherwise
 * @c: variable representing the character
 */
int _islower(int c)
{
	return islower(c);
}
