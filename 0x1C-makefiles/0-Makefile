# My first makefile where
# executable is school
# rules are all
# variables are none

objects = a.out m.h main.c school.c

all: school

school: $(objects)
	gcc $(objects) -o school

clean:
	rm -f school
