#include <stdio.h>
#include <string.h>

/**
 * _strlen - a function that returns length of a string
 * @s: character array
 * Return: length of string
 */

int _strlen(char *s)
{
	return strlen(s);
}
